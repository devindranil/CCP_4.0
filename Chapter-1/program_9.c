#include<stdio.h>
#include<math.h>
int main()
{
    float n, r;
    printf("Enter the number: \n");
    scanf("%f",&n);
    r = sqrt(n);
    printf("Square Root of no %f is %f",n,r);
    
    return 0;
}